import type { TpColor } from '../../../../utils/types/color.type';
export type TpBaseType = {
    color: `${TpColor}`;
    radius: string;
    gap: string;
    spacingV: string;
    spacingH: string;
};
