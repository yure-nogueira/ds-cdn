import type { TpColor } from '../../../../utils/types/color.type';
export type TpTilesType = {
    color: `${TpColor}`;
    titulo: string;
    label: string;
    selected: boolean;
    solid: boolean;
};
