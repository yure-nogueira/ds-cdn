import type { TpColor } from '../../../../utils/types/color.type';
import type { TpIonIconSize } from './icon.enum';
export type TpIonIcon = {
    color: `${TpColor}`;
    'tp-size': `${TpIonIconSize}`;
};
